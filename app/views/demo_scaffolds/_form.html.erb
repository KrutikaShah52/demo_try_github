<%= form_with(model: demo_scaffold, local: true) do |form| %>
  <% if demo_scaffold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(demo_scaffold.errors.count, "error") %> prohibited this demo_scaffold from being saved:</h2>

      <ul>
      <% demo_scaffold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :demo_scaffold_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
